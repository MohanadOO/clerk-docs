---
title: Organization Invitations
description: These are all methods on the Organization class that allow you to manage the invitations to an organization.
---

import { Tables } from "@/components/Table";
import { Images } from "@/components/Images";
import { CodeBlockTabs } from "@/components/CodeBlockTabs";

# Organization Invitations

These are all methods on [the Organization class](/docs/references/javascript/organization/organization) that allow you to manage the invitations to an organization.

## `getInvitations()`

```typescript
function getInvitations(params?: GetInvitationsParams): Promise<PaginatedResources<OrganizationInvitation>>;
```

### `GetInvitationsParams`

| Name | Type | Description |
| --- | --- | --- |
| `initialPage?` | number | A number that can be used to skip the first n-1 pages. For example, if `initialPage` is set to 10, it is will skip the first 9 pages and will fetch the 10th page. |
| `pageSize?` | number | A number that indicates the maximum number of results that should be returned for a specific page. |
| `status?` | `'pending' \| 'accepted' \| 'revoked'` | The status an invitation can have. |

### Returns

<Tables
    headings={["Type", "Description"]}
    headingsMeta={[{}, {maxWidth: "300px"}]}
    rows={[
        {cells: [
                <code>Promise{"<"}<a href="/docs/references/javascript/types/paginated-response">PaginatedResponse</a>{"<"}<a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a>{">"}</code>,
                <>This method returns a <code>Promise</code> which resolves with a list of <code><a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a></code> objects.</>,
            ]},
    ]}
/>


## `getPendingInvitations()` (deprecated)

```typescript
function getPendingInvitations(params?: GetPendingInvitationsParams): Promise<OrganizationInvitation[]>;
```

Retrieve the members of the selected organization.

### `GetPendingInvitationsParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [
  <code>limit</code>,
  <code>number | undefined</code>,
  "Limit of the results returned.",
  ]},
  {cells: [
  <code>offset</code>,
  <code>number | undefined</code>,
  "The offset of results to start the retrieval.",
  ]},
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}<a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a>[]{">"}</code>,
  <>This method returns a <code>Promise</code> which resolves with a list of <code><a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a></code> objects.</>,
  ]},
  ]}
/>

## `inviteMember()`

```typescript
function inviteMember(params: InviteMemberParams): Promise<OrganizationInvitation>;
```

Creates and sends an invitation to the target email address for becoming a member with the role passed on the  function parameters.

### `InviteMemberParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [
  <code>emailAddress</code>,
  <code>string</code>,
  "The email address to invite.",
  ]},
  {cells: [
  <code>role</code>,
  <code>'admin' | 'basic_member' | 'guest_member'</code>,
  "The role of the new member.",
  ]},
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}<a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a>{">"}</code>,
  <>This method returns a <code>Promise</code> which resolves to the <code><a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a></code> for the created invitation.</>,
  ]},
  ]}
/>

## `inviteMembers()`

```typescript
function inviteMembers(params: InviteMembersParams): Promise<OrganizationInvitation[]>;
```

Creates and sends an invitation to the target email addresses for becoming a member with the role passed on the function parameters.

### `InviteMembersParams`

<Tables
  headings={["Name", "Type", "Description"]}
  headingsMeta={[{maxWidth:'300px'},{maxWidth: '300px'},{maxWidth: '300px'}]}
  rows={[
  {cells: [
  <code>emailAddresses</code>,
  <code>string[]</code>,
  "The email addresses to invite.",
  ]},
  {cells: [
  <code>role</code>,
  <code>'admin' | 'basic_member' | 'guest_member'</code>,
  "The role of the new members.",
  ]},
  ]}
/>

### Returns

<Tables
  headings={["Type", "Description"]}
  headingsMeta={[{}, {maxWidth: "300px"}]}
  rows={[
  {cells: [
  <code>Promise{"<"}<a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a>[]{">"}</code>,
  <>This method returns a <code>Promise</code> which resolves to a list of <code><a href="/docs/references/javascript/organization-invitation">OrganizationInvitation</a></code>s for the created invitations.</>,
  ]},
  ]}
/>
